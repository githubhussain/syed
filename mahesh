pipeline(){
    agent any
    stages{
        stage('build'){
            steps{
                sh 'cat /etc/os-release'
            }
        }
        stage('test'){
            steps{
                sh 'df -h'
            }
        }
    }
}
