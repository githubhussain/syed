pipeline {
  agent any
  stages {
    stage('stage'){
      steps {
        sh 'ifconfig'
      }
    }

    stage('stage'){
      steps {
        sh 'cat /etc/os-release'
      }
    }

    stage('stage'){
      steps {
        sh 'hostname -I'
      }
    }

  }
}
